import { BaseConstantModel } from './base-constant-model';

describe('BaseConstantModel', () => {
  it('should create an instance', () => {
    expect(new BaseConstantModel()).toBeTruthy();
  });
});
